// Mock API handlers disabled: the frontend now talks to the real backend
// (see VITE_API_URL / src/services/apiClient.ts). Intentionally empty so no
// request is intercepted by fake data.
export const handlers = [];
